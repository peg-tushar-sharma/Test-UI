import { industry } from "../shared/interfaces/industry";
import { sector } from "../shared/interfaces/sector";
import { subSector } from "../shared/interfaces/subSector";

export class DealCapabilities {
    industries: industry;
    sectors: sector;
    subSectors: subSector;
}