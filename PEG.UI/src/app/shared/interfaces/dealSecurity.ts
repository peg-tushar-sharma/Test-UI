// Interface is used to get data to set deal authorization
export interface DealSecurity {
    name:string;
    tabName:string;
    isTabVisible:boolean;
    isTabReadOnly:boolean;
    isVisible:boolean;
    isEditable:boolean;
    isNone:boolean;
}